<body>
  <%- include('header') %>
  <h1><%= title %></h1>
  <p>&nbsp;</p>
  <main>
    <ul>
      <% items.forEach(item => { %>
        <li>
          <h2><%= item.name %></h2>
          <p><%- item.content.replace(/-/g, '<br>-') %></p>
        </li>
      <% }) %>
    </ul>
  </main>
</body>