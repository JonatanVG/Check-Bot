<!DOCTYPE html>
<html lang="en">
<head>
    <title><%= title %></title>
</head>
  <body>
    <%- include('partials/header') %>
    <h1><%= title %></h1>
    <p>&nbsp;</p>
    <main>
      <ul>
        <% items.forEach(item => { %>
          <li>
            <a href="<%= item.content %>">
              <h2><%= item.name %></h2>
            </a>
          </li>
        <% }) %>
      </ul>
    </main>
  </body>
</html>